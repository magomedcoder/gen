listen:
  host: "127.0.0.1"
  port: 50052

core:
  host: "127.0.0.1"
  port: 50051

registration_token: ""

log_level: "debug"

model_path: "./models"

# mmproj_path: ""

default_model: ""

max_context_tokens: 4096

# 0 - только CPU
# -1 - все слои в VRAM (make build-gpu + libllama с GGML_CUDA)
gpu_layers: 0

max_concurrent_generations: 0

model_retention: keep
